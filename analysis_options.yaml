include: package:flutter_lints/flutter.yaml

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
analyzer:
  # OhosView 只存在于 Flutter OHOS 分支。普通 Flutter 分析时跳过这个很薄的适配入口，
  # Android/iOS 用户仍然可以正常解析和编译主入口。
  exclude:
    - lib/gromore_ads_kit_ohos.dart
    # 子包各自有独立的 pubspec 和分析配置，避免核心包用错误的 package_config 分析。
    - packages/**
