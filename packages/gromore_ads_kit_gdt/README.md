# gromore_ads_kit_gdt

`gromore_ads_kit` 的优量汇（GDT）可选适配包。

```yaml
dependencies:
  gromore_ads_kit: ^2.0.0
  gromore_ads_kit_gdt: ^1.0.0
```

只需要添加依赖，不需要单独初始化优量汇。GroMore 会按照后台瀑布流配置调用它。

支持 Android、iOS 和 HarmonyOS。版本必须与本仓库文档列出的 GroMore 生成包保持一致。
