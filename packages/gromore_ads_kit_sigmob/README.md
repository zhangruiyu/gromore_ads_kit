# gromore_ads_kit_sigmob

`gromore_ads_kit` 的 Sigmob 可选适配包。

```yaml
dependencies:
  gromore_ads_kit: ^2.0.0
  gromore_ads_kit_sigmob: ^1.0.0
```

只需要添加依赖，不需要单独初始化 Sigmob。GroMore 会按照后台瀑布流配置调用它。

当前支持 Android 和 iOS；GroMore 暂未提供对应的 HarmonyOS Adapter。
